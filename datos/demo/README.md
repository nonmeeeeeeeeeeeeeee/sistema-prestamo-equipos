# Datos de demostracion

Archivos JSON semilla que carga `python -m prestamos init-demo`.

Pendiente: usuarios.json, equipos.json, solicitudes.json.
Las contrasenas de demo deben ir documentadas en el README principal.
